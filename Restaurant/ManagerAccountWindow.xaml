<Window x:Class="Restaurant.ManagerAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant"
        mc:Ignorable="d"
        Title="Quáº£n LÃ½ NhÃ¢n Sá»±" Height="600" Width="1000" WindowStartupLocation="CenterScreen"
        Closed="Window_Closed">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Danh sÃ¡ch TÃ i khoáº£n" Grid.Column="0" Margin="0,0,10,0" FontSize="14" FontWeight="Bold">
            <DataGrid Name="dgAccounts" AutoGenerateColumns="False" IsReadOnly="True" 
                      SelectionMode="Single" SelectionChanged="dgAccounts_SelectionChanged"
                      Margin="5" TextElement.FontSize="13" TextElement.FontWeight="Normal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="TÃªn ÄÄƒng Nháº­p" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Há» vÃ  TÃªn" Binding="{Binding FullName}" Width="1.5*"/>
                    <DataGridTextColumn Header="Chá»©c vá»¥" Binding="{Binding RoleNavigation.RoleName}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="1.5*"/>
                    <DataGridTextColumn Header="Tráº¡ng thÃ¡i" Binding="{Binding Status}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <GroupBox Header="ThÃ´ng tin chi tiáº¿t" Grid.Column="1" FontSize="14" FontWeight="Bold">
            <StackPanel Margin="10" TextElement.FontSize="13" TextElement.FontWeight="Normal">

                <TextBlock Text="TÃªn Ä‘Äƒng nháº­p (Username):" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Name="txtUsername" Height="30" Padding="5" Margin="0,0,0,10"/>

                <TextBlock Text="Máº­t kháº©u:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <PasswordBox Name="txtPassword" Height="30" Padding="5" Margin="0,0,0,10"/>

                <TextBlock Text="Há» vÃ  tÃªn:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Name="txtFullName" Height="30" Padding="5" Margin="0,0,0,10"/>

                <TextBlock Text="Email liÃªn há»‡:" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Name="txtEmail" Height="30" Padding="5" Margin="0,0,0,10"/>

                <TextBlock Text="Chá»©c vá»¥ (Role):" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <ComboBox Name="cbRole" Height="30" Padding="5" Margin="0,0,0,15" DisplayMemberPath="RoleName" SelectedValuePath="RoleId"/>

                <TextBlock Text="Tráº¡ng thÃ¡i (1: Hoáº¡t Ä‘á»™ng, 0: KhÃ³a):" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Name="txtStatus" Height="30" Padding="5" Margin="0,0,0,20"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Name="btnAdd" Content="THÃŠM Má»šI" Grid.Row="0" Grid.Column="0" Margin="0,0,5,5" Height="35" Background="#4CAF50" Foreground="White" FontWeight="Bold" Click="btnAdd_Click"/>
                    <Button Name="btnUpdate" Content="Cáº¬P NHáº¬T" Grid.Row="0" Grid.Column="1" Margin="5,0,0,5" Height="35" Background="#FF9800" Foreground="White" FontWeight="Bold" Click="btnUpdate_Click"/>

                    <Button Name="btnDelete" Content="KHÃ“A / Má»ž" Grid.Row="1" Grid.Column="0" Margin="0,5,5,0" Height="35" Background="#F44336" Foreground="White" FontWeight="Bold" Click="btnDelete_Click"/>
                    <Button Name="btnClear" Content="LÃ€M Má»šI FORM" Grid.Row="1" Grid.Column="1" Margin="5,5,0,0" Height="35" Background="#9E9E9E" Foreground="White" FontWeight="Bold" Click="btnClear_Click"/>
                    <Button x:Name="btnClose" Content="ÄÃ³ng" Width="120" Height="40" 
                            Background="#757575" Foreground="White" FontWeight="Bold" FontSize="14"
                            Click="btnClose_Click" Grid.ColumnSpan="2" Margin="120,40,120,-40" Grid.Row="1"/>
                </Grid>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
