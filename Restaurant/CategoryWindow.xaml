<Window x:Class="Restaurant.CategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Restaurant"
        mc:Ignorable="d"
        Title="Category" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="dgCategories" Grid.Column="0" AutoGenerateColumns="False"
                  CanUserAddRows="False" Margin="0,0,10,0" SelectionChanged="dgCategories_selectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ma danh muc" Binding="{Binding CategoryId}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Ten danh muc" Binding="{Binding CategoryName}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1" Orientation="Vertical">
            <TextBlock Text="THÃ”NG TIN DANH Má»¤C" FontWeight="Bold" FontSize="16" Margin="0,0,0,20"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Ma danh muc:" />
            <TextBox x:Name="txtCategoryId" Height="30" Margin="0,5,0,15" IsReadOnly="True" Background="#F0F0F0"/>
            <TextBlock Text="Ten danh muc" />
            <TextBox x:Name="txtCategoryName" Height="30" Margin="0,5,0,20" />
            <WrapPanel HorizontalAlignment="Center">
                <Button x:Name="btnAdd" Content="Them" Width="70" Height="35" Margin="5" Click="btnAdd_Click"/>
                <Button x:Name="btnUpdate" Content="Sua" Width="70" Height="35" Margin="5" Click="btnUpdate_Click"/>
                <Button x:Name="btnDelete" Content="Xoa" Width="70" Height="35" Margin="5" Click="btnDelete_Click"/>
            </WrapPanel>
            <Button x:Name="btnClear" Content="Lam moi" Height="35" Margin="5,15,5,0" Click="btnClear_Click"/>
            <Button x:Name="btnBack" Content="â¬… Quay láº¡i" Height="35" Margin="5,15,5,0" Click="btnBack_Click" Background="#7F8C8D" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>
